[phases.setup]
nixPkgs = ["nodejs_20", "python3", "pkg-config", "cairo", "pango", "libpng", "libjpeg", "giflib", "librsvg"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[start]
cmd = "node index.js"
